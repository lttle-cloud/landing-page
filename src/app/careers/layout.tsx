export const metadata = {
    title: "Careers | lttle.cloud",
    description:
        "Join our team and help us build the future of cloud computing",
};

export default function CareersLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <div>{children}</div>;
}
