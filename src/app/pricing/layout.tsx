export const metadata = {
    title: "About Us | lttle.cloud",
    description: "Learn more about our company and mission",
};

export default function AboutUsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <div>{children}</div>;
}
